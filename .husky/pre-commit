if git diff --cached --name-only | grep -q 'package\.json'; then
  yarn install
  git add yarn.lock
fi

STAGED=$(git diff --cached --name-only --diff-filter=d | grep -E '\.(js|ts|mjs)$' || true)
if [ -n "$STAGED" ]; then
  echo "$STAGED" | xargs yarn eslint
fi
